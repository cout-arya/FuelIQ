FROM node:20-alpine
WORKDIR /app
COPY server/package*.json ./
RUN npm ci --only=production
COPY server/ ./
HEALTHCHECK CMD wget --no-verbose --tries=1 --spider http://localhost:5001/api/health || exit 1
EXPOSE 5001
CMD ["node", "index.js"]
